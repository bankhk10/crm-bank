/**
 * Customer Form Types
 * Re-export from types.ts for backward compatibility
 * @deprecated Use imports from "./types" instead
 */

export type { CustomerPayload, SubmitResult, CustomerFormProps } from "./types";
